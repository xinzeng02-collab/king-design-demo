{
  "name": "king-design-payment-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "KiNG DESiGN 支付模块后端 —— 统一 Payment Provider 适配层 + Cloudflare Workers + Supabase",
  "scripts": {
    "test": "node --test",
    "smoke": "node test/smoke.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
    ,"create-admin": "node scripts/create-admin.mjs",
    "initialize:test-workspace": "node scripts/initialize-test-workspace.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
