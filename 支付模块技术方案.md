# KiNG DESiGN 线上支付模块 · 技术方案 V1

> 状态：方案评审阶段（未开始实现）。确认后再进入编码。

---

## 0. 一句话结论

当前 KiNG DESiGN 是一个**纯前端原型**（单页 HTML/JS + 浏览器 localStorage 假数据 + 假登录）。
要做**真实收款**的支付模块，本质是**新建一整套生产后端**（Supabase + Cloudflare Workers + R2 + 真实鉴权），
再把前端接到后端。这不是在原型上"加一个支付页"，是一个**多阶段的后端工程**。
在没有真实商户密钥前，可以先把"支付页面 / 数据库结构 / Provider 适配层 / 模拟支付 / 回调骨架 / 状态机 / 对公转账 / 受控交付 / 自动化测试"全部做出来，
**但模拟支付会明确标注为测试模式，绝不伪造真实支付成功。**

---

## 1. 现状调研结论

| 维度 | 现状 |
|---|---|
| 前端 | `studio-site/index.html` + `script.js`(~1万行) + `styles.css`，无构建、无框架、无 npm |
| "数据库" | 浏览器 localStorage（主键 `studio_site_design_ops_v2`）+ IndexedDB（图片）。**纯客户端，可被任意篡改** |
| 鉴权 | 前端假登录，密码按客户编号确定性生成，**无服务端校验、无 RLS** |
| 订单 | `studioOrders`，字段 `id/customer/viewer/status/progress/deliverStatus/agreementStatus/price/patternIds/files...`；`price = 花型数×100` 的普通数字，**非整数分** |
| 支付 | **完全不存在**：无支付页、无 payments/refunds 表、无回调 |
| 交付 | 订单中心一个"交付"按钮手动切状态；客户端能看已交付花型（但走的是本地缩略图，非受控下载） |
| 基础设施 | **无** Supabase / Cloudflare / R2 / 域名 / 备案 |

**含义**：支付、金额、订单状态一旦放到"可被客户端改"的 localStorage 里，等于零安全。支付模块必须建立在**服务端权威**之上。

---

## 2. 方案 A vs 方案 B（必须先定）

### 方案 A：分别直连微信支付 / 支付宝 / 银联 / Apple Pay 官方接口
- 每条通道各自独立：各自的商户号、资质审核、API 密钥/证书、签名规则、回调格式、对账文件、退款接口。
- 优点：费率可能更低；数据最原始。
- 缺点：**四套接入 + 四套合规 + 四套对账**，工作量与维护成本极高；小工作室通常没有对应的合规与运维投入；Apple Pay 在中国大陆本身还是跑银联/微信/支付宝通道，单独"直连"意义不大。

### 方案 B：接入一个合规聚合支付服务商（PSP），由它统一处理各渠道
- 一次接入，拿到微信/支付宝/银联/(可能)Apple Pay + 卡组织；一套结算、一套对账。
- 优点：上线快、合规负担小、维护简单、Apple Pay 由 PSP 统一支持。
- 缺点：费率略高、部分原始字段被 PSP 归一化。

### 建议：**方案 B 起步，架构上预留方案 A**
理由：你们是设计工作室体量，方案 B 能最快合规上线；而我设计的**统一 Payment Provider 适配层**让"用 PSP"还是"某条通道直连"对业务代码透明，将来量大了想直连某条通道，只需新增一个 Provider，不改业务。

### 关键分叉：**收款主体在哪、收什么币种？**（这决定选哪个 PSP，必须你先回答）
- **中国大陆主体、收人民币**：用国内 PSP（如 Ping++/连连/汇付/富友 等，需自行核定资质与费率）。Stripe 不支持大陆主体做境内人民币收款。
- **境外主体 / 收外币（你们有加拿大、澳洲、日本客户）**：**Stripe** 极合适——一套接入即支持 Alipay、WeChat Pay、UnionPay、Apple Pay、信用卡，文档好、与 Cloudflare Workers 天然兼容。
- **两者都要**：大概率需要"国内 PSP（收人民币）+ Stripe（收外币）"双通道，适配层正好用来屏蔽差异。

> 我需要你明确：**主要收款主体是大陆公司收人民币，还是境外/外币，还是两者都要？** 这一条不定，PSP 选型就定不了。

---

## 3. 各支付方式的真实接入通道（不做假按钮）

| 支付方式 | 真实通道 | 关键前置 |
|---|---|---|
| 微信支付 | PSP 的微信通道，或微信支付商户号直连（Native 扫码/H5/JSAPI） | 商户主体 + **ICP 备案域名**；PC 端优先返回二维码 |
| 支付宝 | PSP 的支付宝通道，或支付宝直连（电脑网站支付/手机网站支付/当面付） | 商户主体 + **备案域名**；PC 可跳收银台或返回二维码 |
| 银联 | PSP 的银联通道，或银联商务/网联 | 商户号 + 证书；跳银联收银台 |
| Apple Pay | **不是独立支付公司**，是卡的呈现方式，必须由支持它的 PSP（如 Stripe/Adyen；大陆走银联）承载 | **域名验证文件** `apple-developer-merchantid-domain-association`、Apple Merchant ID、HTTPS 真实域名、且 PSP 支持 Apple Pay |
| 对公转账 / 线下 | 无需支付渠道，纯业务流程：客户上传凭证 → 财务人工确认到账 | 对公银行账户信息 |

> **Apple Pay 明确**：只有在"设备/浏览器支持 + 你的域名通过验证 + PSP 实际支持"三者同时满足时才显示按钮；否则**隐藏入口**，绝不显示一个点了没用或伪造成功的假按钮。无法真实接入时，保留统一适配接口即可。

---

## 4. 需要你提供 / 申请的商户资质与账号（清单）

**主体与合规（必须，缺了任何支付都做不了真实收款）**
- [ ] 营业执照（收款商户主体）
- [ ] 法人身份信息 / 联系人
- [ ] 对公银行账户（结算 + 对公转账收款账户）
- [ ] **一个真实域名 + 中国大陆 ICP 备案**（微信/支付宝网页收银台、Apple Pay 域名验证都要求）

**支付通道（按方案 B，二选一或都要）**
- [ ] 选定的 PSP 商户账号 + API Key/Secret + 回调密钥（Stripe / 国内 PSP）
- [ ] （若走方案 A）微信支付：商户号 mch_id + APIv3 密钥 + 商户证书/私钥 + APPID
- [ ] （若走方案 A）支付宝：AppID + 应用私钥 + 支付宝公钥
- [ ] （若走方案 A）银联：商户号 + 证书

**Apple Pay（如需）**
- [ ] Apple Developer 账号 + Merchant ID + 域名验证文件部署到你的域名

**基础设施**
- [ ] Cloudflare 账号（Pages + Workers + **R2 bucket**）
- [ ] Supabase 项目（数据库 + Auth + Service Role Key）
- [ ] GitHub 仓库
- [ ] NAS 访问方式（原始 PSD/TIFF/AI 备份，非支付关键路径）

---

## 5. 现在就能做 vs 必须等资质密钥

**没有真实密钥，现在就能完整交付（且不假装真支付）：**
1. Supabase 数据库结构与迁移（orders / payments / payment_events / refunds / bank_transfer_records / audit_logs），金额一律整数分、三状态分离
2. 统一 **Payment Provider 适配层**（接口 + `MockProvider` 沙箱）
3. 支付页面 UI + 全部支付状态组件（含倒计时、按设备切换体验）
4. Cloudflare Workers/Pages Functions 后端**路由骨架**与业务层
5. 回调接口骨架（签名验证/幂等/事件日志的结构，先接 Mock）
6. 支付状态机与"服务端权威"落地
7. 对公转账全流程（本就是人工流程，可完整跑通）
8. 付款后**受控交付**（R2 短期签名链接的逻辑，可先用测试桶）
9. Supabase RLS 权限与 audit_logs
10. 自动化测试（第九节清单）

> 模拟支付一律带 `MOCK / TEST` 标记，正式环境不启用。

**必须等你给到资质/密钥才能做的：**
- 真实拉起微信/支付宝/银联/Apple Pay 收款
- 真实回调验签（需要真实商户密钥/证书）
- Apple Pay 域名验证与真实收款
- 生产 R2 桶、生产 Supabase、生产域名部署

---

## 6. 目标架构

```
GitHub ──> Cloudflare Pages (员工端/客户端前端，静态)
              │  fetch /api/*
              ▼
        Cloudflare Workers / Pages Functions (支付&业务后端)
        ├─ Payment Provider 适配层 (Mock/WeChat/Alipay/UnionPay/ApplePay/BankTransfer)
        ├─ 回调验签 / 主动查单 / 幂等入账
        ├─ 受控下载签名 (R2)
        └─ 权限校验 (Supabase JWT)
              │
     ┌────────┼─────────────┐
     ▼        ▼             ▼
 Supabase   Cloudflare R2   NAS
 (数据/RLS) (预览图/交付件)  (原始文件备份)
```

### 6.1 统一 Payment Provider 适配层（核心）
每个 Provider 实现同一接口，业务层只认内部统一结构：
```
interface PaymentProvider {
  createPayment(order, method, ctx): Promise<InternalPayment>   // 返回二维码/跳转URL/ApplePaySession等
  queryPayment(paymentId): Promise<InternalPaymentStatus>       // 主动查单（权威来源之一）
  verifyNotification(req): Promise<VerifiedEvent | null>        // 验签 + 商户号/APPID/金额/币种/订单核对
  handleNotification(event): Promise<void>                      // 幂等入账 + 写 payment_events
  closePayment(paymentId): Promise<void>
  createRefund(payment, amount, reason): Promise<InternalRefund>
  queryRefund(refundId): Promise<InternalRefundStatus>
}
```
`WeChatPayProvider / AlipayProvider / UnionPayProvider / ApplePayProvider / BankTransferProvider / MockProvider` 各自实现，
渠道返回统一转换为内部结构（金额→分、状态→内部枚举）。业务代码永远不写"if 微信 else 支付宝"。

---

## 7. 数据库设计（Supabase / Postgres）

**原则：金额一律 `bigint` 存最小单位（分）；订单/支付/交付三状态分离；所有敏感变更进 `audit_logs`。**

- `orders`：id, order_number, organization_id, customer_id, currency, subtotal_amount(分), discount_amount(分), payable_amount(分), order_status, payment_status, delivery_status, expires_at, created_at, updated_at
- `payments`：id, order_id, payment_number, provider, payment_method, provider_transaction_id, amount(分), currency, status, paid_at, expired_at, raw_response(jsonb), created_at, updated_at
- `payment_events`：id, payment_id, event_type, provider_event_id, verification_status, payload(jsonb), processed_at, created_at（`provider_event_id` 唯一，用于幂等）
- `refunds`：id, payment_id, refund_number, provider_refund_id, amount(分), status, reason, refunded_at, created_at, updated_at
- `bank_transfer_records`：id, order_id, payment_proof_url, payer_name, transfer_reference, submitted_at, confirmed_by, confirmed_at, status, remark
- `audit_logs`：id, actor_id, actor_role, action, target_type, target_id, before(jsonb), after(jsonb), created_at
- 交付相关：`delivery_files`（order_id, r2_key, version, file_type）+ `download_tokens`（记录下载人/时间/次数/过期）

**状态枚举（按你的规范）**
- order_status：draft / pending_confirmation / active / completed / cancelled / expired
- payment_status：unpaid / processing / paid / partially_refunded / refunding / refunded / failed / expired
- delivery_status：not_ready / preparing / ready / downloaded / frozen / revoked

**从现有原型迁移**：把 localStorage 里的 `studioOrders` 一次性导入 `orders`（`price×100` 转为分；`deliverStatus` 映射到 `delivery_status`；补 `payment_status=unpaid`）。

---

## 8. 安全与"支付成功以服务端为准"

- 密钥/证书/Service Role Key 全部放 **Cloudflare Secrets**；绝不进 GitHub / 前端 / localStorage / 公开环境变量 / 客户端可读字段。
- **判定支付成功只认三者之一**：①服务端收到并**验签通过**的异步通知；②服务端**主动查单**确认；③对公转账由**财务权限**员工人工确认。
- 前端跳到 success 页 / URL 带 success / 前端改 payment_status / 客户上传截图 / 未验证的轮询数据 —— **一律不算**。
- 回调处理必做：签名验证、商户号核对、APPID 核对、订单号核对、**金额核对、币种核对**、状态核对、`provider_event_id` 幂等、事件日志、异常记录、事务一致性。**同一笔多次通知只入账一次。**

---

## 9. 付款后受控交付

客户请求下载时，后端**每次重新校验**：登录有效 + 属于该客户公司 + 订单属于该公司 + `payment_status=paid` + `delivery_status=ready` + 无退款/未冻结 + 授权已生效 + 文件确属该订单。
通过后才为 R2 文件生成**15–30 分钟短期签名链接**；记录下载人/时间/文件版本、可限次；退款/冻结/撤销授权后停止发新链接；**不在库或前端存永久公开地址**。

---

## 10. 对公转账流程
客户：查看收款账户 → 上传凭证 + 付款公司/付款人/银行流水号 → 查看确认状态。
财务：查看待确认 → 填实际到账金额/时间 → 确认或驳回 → 记录流水凭证。
**只有财务/管理员/老板能确认到账**；销售/设计师/普通员工**不能**把订单改成已支付。所有确认写 audit_logs。

---

## 11. 权限（Supabase RLS + 服务端）
客户只看本公司订单与付款、不能改金额/支付状态；销售建/调未确认报价；财务确认对公转账与退款；管理员看全量；设计师只看交付必要信息。所有金额调整/付款确认/退款/交付写 audit_logs。

---

## 12. 测试清单（自动化，覆盖第九节全部）
成功（微信/支付宝/银联/ApplePay）、取消、二维码过期、超时、重复通知幂等、**伪造通知/验签错误/金额不一致（必须拒绝）**、支付成功但落库失败、库更新但前端未刷新、重复点击/一单多支付、已取消订单收到通知、退款(部分/全额)、退款后仍请求下载(拒绝)、未付款强行调下载(拒绝)、前端改 payment_status(无效)、越权访问他人订单(拒绝)、对公凭证驳回、财务重复确认(幂等)。

---

## 13. 部署与环境变量
- 前端：Cloudflare Pages（连 GitHub 自动部署）
- 后端：Cloudflare Workers / Pages Functions；密钥用 `wrangler secret put`
- 数据库：Supabase（迁移脚本 + RLS 策略）
- 文件：Cloudflare R2（预览图/交付件）
- 提供 `.env.example`（只给变量名，**不含任何真实密钥**）

---

## 14. 分阶段实施计划（确认后执行）
1. 搭后端骨架（Workers 项目 + Supabase 迁移 + Mock Provider）
2. 支付页面 + 状态机（接 Mock，全流程可点可测）
3. 对公转账全流程（可真实跑通）
4. 受控交付（R2 签名，先测试桶）
5. 回调/查单/退款骨架 + 幂等 + 验签结构
6. RLS + audit_logs + 自动化测试
7. **接第一个真实通道**（拿到密钥后，先沙箱后生产）
8. Apple Pay（拿到域名验证后）
9. 灰度上线 + 对账

> 全程**不破坏**现有员工端/客户端/订单/花型管理功能；老原型可与新后端并行，逐步切换。

---

## 需要你先拍板的三件事
1. **收款主体与币种**：大陆收人民币 / 境外收外币 / 两者都要？（决定 PSP 选型）
2. **方案 A 还是 B**：建议 B 起步（我已按 B 设计，适配层预留 A）。
3. **是否先做"测试模式全链路"**：在你办资质/密钥期间，我先把页面+库+适配层+Mock+对公+受控交付+测试全部做出来。
